import {
    LOGIN_START,
    LOGIN_SUCCESS,
    LOGIN_FAILED
} from '../actions/actions'


const initialState = {
    friends: [],
    isLoading: false,
    error: null
}

const reducer = (state = initialState, action) => {
    return state;

}

export default reducer;